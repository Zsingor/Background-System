<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springtest.mapper.ConversationsMapper">
    <select id="getContactUser" resultType="com.example.springtest.entity.User">
        select u.id,u.name,u.description
        from test.conversations c left join test.user u on c.contact_id = u.id
        where c.user_id=#{userId}
        order by c.update_time desc
    </select>
</mapper>